#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../.deno/jsesc@3.1.0/node_modules/jsesc/bin/jsesc" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../.deno/jsesc@3.1.0/node_modules/jsesc/bin/jsesc" $args
  }
  $ret=$LASTEXITCODE
} else {
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../.deno/jsesc@3.1.0/node_modules/jsesc/bin/jsesc" $args
  } else {
    & "node$exe"  "$basedir/../.deno/jsesc@3.1.0/node_modules/jsesc/bin/jsesc" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
